dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../../../../gradle/libs.versions.toml"))
    }
  }
}


includeBuild('../../../../../') {
  dependencySubstitution {
    substitute module('app.cash:better-dynamic-features') using project(':gradle-plugin')
  }
}
