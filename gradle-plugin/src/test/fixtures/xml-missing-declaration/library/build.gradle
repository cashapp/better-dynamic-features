apply plugin: 'com.android.library'
apply plugin: 'org.jetbrains.kotlin.android'

android {
  namespace "app.cash.better.dynamic.features.integration.library"

  compileSdk 34

  defaultConfig {
    minSdk 24
  }
}

kotlin {
  jvmToolchain(17)
}

dependencies {
  implementation libs.androidx.core
}
